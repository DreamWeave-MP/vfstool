[package]
name = "vfstool_lib"
version = "1.0.0"
edition = "2024"
rust-version = "1.87"
description = "A library for constructing and manipulating virtual file systems in Rust, based on OpenMW's VFS implementation."
license = "GPL-3.0-only"
readme = "README.md"
homepage = "https://github.com/DreamWeave-MP/vfstool"
repository = "https://github.com/DreamWeave-MP/vfstool"
keywords = ["vfs", "openmw", "gamedev", "modding", "bethesda"]

[dependencies]
ahash = "0.8"
blake3 = "1"
dream-path = "0.1.3"
dream_archive = { version = "0.1.5", optional = true, default-features = false, features = ["ba2", "bsa", "parallel"] }
mlua = { version = "0.11.6", default-features = false, features = ["serialize"], optional = true }
zip = { version = "8", optional = true, default-features = false, features = ["deflate", "lzma"] }
rayon = "1.11.0"
regex = "1"
serde = { version = "1.0.225", features = ["derive"], optional = true }
serde_json = { version = "1.0.145", optional = true }
serde_yaml = { version = "0.9.34-deprecated", optional = true }
toml = { version = "1.1.2", optional = true }
walkdir = "2.5.0"

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[features]
default = []
beth-archives = ["dep:dream_archive"]
zip = ["dep:zip"]
serialize = ["dep:serde", "dep:serde_json", "dep:serde_yaml", "dep:toml"]
lua = ["dep:mlua", "mlua/serialize"]
standalone-lua = ["lua", "mlua/luajit52", "mlua/vendored"]

[package.metadata.docs.rs]
features = ["beth-archives", "zip", "serialize", "standalone-lua"]

[[bench]]
name = "vfs_benchmarks"
harness = false
